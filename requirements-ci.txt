# CI test requirements -- CPU-only, minimal footprint for GitHub-hosted runners.
# Excludes: tensorflow, torch, qiskit, qiskit-aer, ray, gradio, moviepy, wandb,
# fastapi, sphinx (~8 GB of wheels) which are not imported anywhere in src/.
# Keep this file in sync with requirements.txt for the deps that ARE imported.

# Core JAX/Flax/Optax stack -- CPU build pinned in workflow with jax[cpu]==0.4.35
# NOTE: dm_haiku 0.0.13 imports jax.core.JaxprEqn which was removed in JAX 0.6 --
# do NOT bump JAX without also bumping haiku.
jax==0.6.2
jaxlib==0.6.2
dm_haiku==0.0.16
optax==0.2.8

# Numerical
numpy>=1.26,<2.1
scipy>=1.11.0,<1.15.0
scikit-learn==1.7.2

# Retrieval / vector store
faiss-cpu==1.14.3

# Serialization & security
safetensors>=0.4.0
cryptography>=42.0.0

# Fairness (imported in src/core/ethics)
fairlearn>=0.10.0

# Visualization (imported in src/train.py)
matplotlib==3.10.9
rich==13.9.4

# NLP utilities (imported in src/modules)
nltk==3.9.4

# Hyperparameter search (imported in src/core)
optuna==4.9.0

# Image / audio (imported in src/modules/multimodal)
Pillow>=8.0,<13.0
opencv-python==4.13.0.92
librosa==0.11.0
soundfile==0.14.0

# Graph reasoning
networkx==3.4.2

# Logging
loguru==0.7.3
psutil==6.1.1

# Config
omegaconf==2.3.1
hydra-core==1.3.3

# HF tooling (transformers tokenizers used in benchmarks/data utils)
transformers==4.47.1

# Experiment tracking (gated import; install for completeness)
mlflow-skinny>=2.10.0

# HTTP client
requests==2.34.2
pydantic==2.13.4

# Test runners
pytest==8.3.4
pytest-cov
pytest-xdist
pytest-timeout
